#ifndef ARBRE_MOVEMENT
#define ARBRE_MOVEMENT

#include "Coord.h"
#include "Piece.h"
#include <vector>


struct Anode
{
  Coord c;
  int prev;
  int* succ;
};
class ArbreMovement
{
 private:
  Piece* p;
  std::vector<Anode> arbre;
 public:
  ArbreMovement(Chessplate*,Type,Coord);
};

#endif
